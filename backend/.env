# Database configuration (Prisma standard)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/campusprep?schema=public"

# Express server port
PORT=5000

# Google Gemini API key (optional - system falls back to mock responses if blank)
GEMINI_API_KEY=""
